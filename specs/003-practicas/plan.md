# 003 — Plan
- Colección `practicas` con `pasos[]`, `momento`, `duracionMin`, `interactiva?`.
- Islas Preact: `CirculoControl.tsx` (pointer events + botones accesibles), `ExamenNocturno.tsx`.
- Persistencia vía `src/lib/diario.ts` (idb-keyval).
